# 开发/打包用依赖（运行时镜像不装）
-r requirements.txt
pyinstaller>=6.10,<7
pillow>=10.4,<11   # scripts/gen-icons.py
pyyaml>=6,<7       # scripts/check-workflows.py
# 本地 NER 引擎（engine/ner_engine.py）依赖：PyInstaller 打进 sidecar 需要它们
# 在构建机可导入，本地跑 NER 单测同理。CPU 版即可（模型在本机推理，零外传）。
# 运行时代码为懒加载 + 全异常兜底：缺依赖时引擎照常工作，只是语义识别不可用，
# 且会在 UI/健康检查里明确报出原因，不会静默降级。
onnxruntime>=1.20,<2.0
tokenizers>=0.20,<1.0
