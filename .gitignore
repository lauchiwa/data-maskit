# 本地可视化预览与测试辅助工具
preview.bat
preview.ps1
scripts/preview-env.py
scripts/preview_env.py
scripts/seed-mock-data.py
scripts/seed_mock_data.py
test_samples/

# Python
__pycache__/
*.py[cod]
*$py.class
*.log
*.pid
*.jsonl
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal
*.backup
*.db
.env
.env.*
config.local*
.venv/
venv/
.venv-test/
build/
dist/
dist_releases/
dist_engine/
build_engine/
dist_extension/
build_temp/
*.spec.bak
proxy_token
# model_rules 的 {{device_id}} 种子：按数据目录生成的机器指纹，绝不入库
model_rules_device_id
# 上游 NER 的 ONNX 模型目录（98MB）与测试虚拟环境
engine/models/
_test_env*
_smoke_data/
ai-coding/
frontend/.playwright-cli/
.coverage
.coverage.*
coverage/
htmlcov/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.tmp
*.temp
test-*.ps1
_docker_test_run/
_tmp*
.turbo/
# 引擎运行时在数据目录生成的文件（开发态数据目录 = engine/）
engine/config.json
config.json.bak-*
model_prices_cache.json
diagnostics-*.json
shield-env-backup.json
debug-*.log

# Node / Frontend
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/.vite/
*.local

# Tauri / Rust
src-tauri/target/
src-tauri/target-build/
src-tauri/gen/
src-tauri/resources/engine/
*.pdb

# Local / IDE
# 本机工作产物与个性化规则
#
# ⚠️ AGENTS.md / CLAUDE.md **不能**忽略：它们是项目的工程规范基准（判据见 AGENTS.md §6.1），
# 克隆体上缺了它们等于整套 red line 整体丢失；CLAUDE.md 只是指向 AGENTS.md 的一行入口。
# 二者曾于本机被误加入本忽略清单，恢复 git 跟踪时请确认它们在 `git ls-files` 里。
PROMOTION_GUIDE.md
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
.claude/
.pi/
.pi-subagents/
.codex/
.workbuddy-ai/
# 与 .workbuddy-ai 同族：AI 助手在本项目的本地记忆（memory/），属本机状态，不入公开仓库
.workbuddy/
.mcp.json
tests/e2e_reports/
tests/e2e_web_console.py


# 本机审计 / 分析产出（非项目资产）：写的是「代码现状 + 当时的判断」，
# 生命周期比代码短，且会随着代码演进迅速过期，留在仓库里只会误导后来者。
# 曾于 d4edbf6 被误纳入跟踪，并因文中引用 PEM 头部字面量把 CI 的 version job 打红。
AUDIT-*.md

# 会话交接文件（自带「接棒 AI 必读」「最后更新: <时间戳>」）：内容是某次会话的
# 进度快照与暂存说明，下一棒接手即作废。判据见 AGENTS.md §6。
HANDOVER.md

# 设计文档：写于改动之前，落地时几乎必然与最终实现有偏差；入库会被后来者
# 当成现状读。当前事实以代码注释与测试为准。判据见 AGENTS.md §6.1。
DESIGN-*.md

# 本机调研 / 方案探索 / 参考快照（统一归档至已忽略的 ai-coding/，或按通配忽略）
docs/*-REFERENCES.md
docs/DESIGN-*.md
docs/AUDIT-*.md
docs/RESEARCH-*.md

